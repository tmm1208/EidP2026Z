{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Ruff Format",
            "type": "shell",
            "command": "ruff format src && ruff check src --fix --unsafe-fixes",
            "problemMatcher": []
        }
    ]
}
